<main>
  <div class="pull-left">
    <h5>注文情報入力</h5>
  </div>
  <div class="conteiner">
      <div class="low">
          <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
            <%= form_with model: @orders, url: new_order_path do |f| %>
              <h5><strong>支払方法</strong></h5>
                  <%= f.label :sales_status_draft, 'クレジットカード' %>
　　　　　　　　　<%= f.radio_button :sales_status, :credit_card %>
              <h5><strong>お届け先</strong></h5>
                  
              <%= f.submit '確認画面へ進む', class: "btn-success" %>
              <% end %>
              </div>
          </div>
      </div>
    </div>
</main>